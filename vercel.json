{
  "buildCommand": "pwd && ls -la apps/ && ls -la apps/web/ && cat apps/web/package.json && cd apps/web && npm run build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "npm install",
  "framework": null
}
